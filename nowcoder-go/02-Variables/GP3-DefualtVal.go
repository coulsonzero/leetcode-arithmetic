package main

import "fmt"

/**
 * Question: GP3 个人信息
 * Description: 每个人都有姓名，年龄，性别，用三个变量，类型分别是string,int,bool，来表示人的这三个基本信息。
 *              然后按照，姓名，年龄，性别的顺序进行输出打印。
 * input : 无
 * output:
 *         ""
 *         0
 *         false
 */

func main() {
	var (
		name   string
		age    int
		gender bool
	)
	fmt.Println(name)
	fmt.Println(age)
	fmt.Println(gender)
}
